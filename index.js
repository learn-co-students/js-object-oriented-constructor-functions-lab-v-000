// define variable
// the function should create a new scooter with three properties (year, color, model)
let Scooter = function Scooter(year, color, model) {
  this.year = year
  this.color = color
  this.model = model
}

// define Driver variable
// create a new driver with three properties
// three properties (name, age, experience)
let Driver = function Driver(name, age, experience) {
  this.name = name
  this.age = age
  this.experience = experience
}

// define PickupLocation
// create PickupLocation variable
// Two properties (address, city)
let PickupLocation = function PickupLocation(address, city) {
  this.address = address
  this.city = city
}
